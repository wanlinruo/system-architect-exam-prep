---
任务编号: E2-03
任务名称: 细化 03-层次式架构设计.md
对应专题: 层次式架构设计
重点级别: 高频
设计日期: 2026-05-03
状态: 待实施
---

# E2-03 层次式架构设计 细化设计文档

## 一、背景与目标

### 1.1 任务来源

依据 [E2 框架阶段计划](../plans/2026-04-30-e2-case-analysis-framework.md)，E2 已建立 9 个专题骨架，本次按方案 1 优先级（高频先行）对 `02-案例分析/03-层次式架构设计.md` 进行细化。

### 1.2 范围决策

经与用户确认，采用 **方案 A 标准细化**：沿用骨架的 X.1-X.4 结构展开，含 1-2 道历年真题完整答题示例。

### 1.3 成功标准

- 覆盖层次式架构的全部高频考点：4 层架构、MVC/MVP/MVVM 对比、物联网 3 层、分层原则
- 含 1-2 道历年真题示例（题目 + 完整答题）
- 章节结构、callout 风格与 E2-02 一致
- 行数约 280-350 行（骨架 111 → 细化版 280+）

---

## 二、文件组织

### 2.1 文件路径

- 路径：`02-案例分析/03-层次式架构设计.md`
- 操作：在现有骨架基础上 ==重写==（保留 frontmatter / 标题 / warning，扩充 mindmap 与 3.1-3.4）

### 2.2 完整结构

```
# 层次式架构设计
├── (frontmatter)
├── ## 知识全景         — Mermaid mindmap
├── ## 3.1 核心概念
│   ├── 3.1.1 4 层架构职责（表现/业务/数据访问/数据）
│   ├── 3.1.2 分层原则与依赖方向
│   └── 3.1.3 严格分层 vs 松散分层
├── ## 3.2 关键技术与架构
│   ├── 3.2.1 MVC / MVP / MVVM 三者对比
│   ├── 3.2.2 物联网 3 层架构
│   └── 3.2.3 分层陷阱（贫血模型 / 过度分层）
├── ## 3.3 典型考点与解题套路
│   ├── 3.3.1 MVx 选择题答题模板
│   ├── 3.3.2 关键字判定速查
│   └── 3.3.3 物联网架构填空套路
├── ## 3.4 真题与练习
│   ├── 3.4.1 真题示例 1（MVx 模式选择）
│   └── 3.4.2 真题示例 2（物联网 3 层填空）
└── ## 关联链接
```

---

## 三、各节内容设计

### 3.1 核心概念

#### 3.1.1 4 层架构职责

| 层级 | 职责 | 典型组件 |
|------|------|---------|
| **表现层** | UI 呈现、用户交互、输入校验 | View / Web 页面 / 移动端界面 |
| **业务层** | 业务逻辑、规则、流程 | Service / Domain / Use Case |
| **数据访问层 DAO** | 持久化访问、ORM、数据查询 | Repository / Mapper / DAO |
| **数据层** | 数据存储 | RDBMS / NoSQL / 文件系统 |

#### 3.1.2 分层原则与依赖方向
- **依赖单向**：==上层依赖下层、下层不能调上层==
- 表现层 → 业务层 → 数据访问层 → 数据层
- 跨层调用风险：耦合提升、不易维护

#### 3.1.3 严格分层 vs 松散分层

| 类型 | 特征 | 优缺点 |
|------|------|--------|
| **严格分层** | 上层只能调相邻下层 | 隔离强、性能稍差 |
| **松散分层** | 允许跨层调用 | 性能好、耦合高 |

**关键 callout**：
- `> [!important]` 4 层架构职责对照
- `> [!warning]` 跨层调用是设计陷阱

### 3.2 关键技术与架构

#### 3.2.1 MVC / MVP / MVVM 对比（必考核心）

| 维度 | MVC | MVP | MVVM |
|------|-----|-----|------|
| 全称 | Model-View-Controller | Model-View-Presenter | Model-View-ViewModel |
| 数据流向 | View → Controller → Model | View ↔ Presenter ↔ Model | View ↔↔ ViewModel ↔ Model（==双向绑定==） |
| View 与 Model 的关系 | View 直接监听 Model | View 完全 ==被动==，由 Presenter 操控 | View 与 ViewModel ==双向数据绑定== |
| UI 测试性 | 一般（View 与 Model 耦合） | **强**（Presenter 可单独测） | **强**（ViewModel 可单独测） |
| 学习曲线 | 低 | 中 | 中-高 |
| 典型框架 | Spring MVC、Struts2、Rails | Android（早期）、GWT | **Vue.js、Angular、WPF、SwiftUI** |
| 适用场景 | 服务端 Web 经典 | Android 应用、富客户端 | Web 单页应用 SPA、桌面/移动跨平台 |

#### 3.2.2 物联网 3 层架构（IoT 必备）

| 层级 | 职责 | 典型技术 |
|------|------|---------|
| **感知层** | 数据采集（最底层） | RFID、传感器、二维码、摄像头 |
| **网络层** | 数据传输 | 5G、Wi-Fi、ZigBee、LoRa、MQTT、NB-IoT |
| **应用层** | 业务处理与服务（最顶层） | 智能家居、智慧城市、工业 4.0、车联网 |

**关键记忆**：感知 → 网络 → 应用（自下而上）。

#### 3.2.3 分层陷阱
- **贫血模型**（Anemic Domain Model）：业务逻辑全在 Service，Domain 只有 getter/setter — 退化为数据搬运
- **过度分层**：增加不必要的层（如简单 CRUD 也强分 4 层）→ 性能差、维护成本高
- **跨层调用**：表现层直接访问数据层，绕过业务层 → 业务规则失控

**关键 callout**：
- `> [!important]` MVC vs MVP vs MVVM 7 维度对比
- `> [!important]` 物联网 3 层架构表
- `> [!warning]` 贫血模型 vs 充血模型

### 3.3 典型考点与解题套路

#### 3.3.1 MVx 选择题 答题模板

> 第一段（理论）：层次式架构的常见 UI 模式有 MVC、MVP、MVVM 三种，主要区别在 ==View 与 Model 的关联方式== 和 ==数据流向==。
>
> 第二段（分点对比）：
> - （1）MVC：View → Controller → Model 单向，适合 ==服务端 Web==（Spring MVC）
> - （2）MVP：View 完全被动，由 Presenter 操控，==Android 经典==
> - （3）MVVM：双向数据绑定，==Vue / Angular / SwiftUI==
>
> 第三段（结合场景）：本系统的核心需求是 <场景关键字>，故选用 <X 模式>。

#### 3.3.2 关键字判定速查

| 题目关键字 | 推荐模式 |
|----------|---------|
| 服务端 Web、Spring、Rails、传统 Web 应用 | **MVC** |
| Android（早期）、富客户端、UI 单元测试 | **MVP** |
| **Vue / Angular / SPA、双向数据绑定、响应式** | **MVVM** |
| 物联网、传感器、设备接入、5G | **物联网 3 层架构** |

#### 3.3.3 物联网架构填空套路
- 题目常给出物联网架构图，让填空"层级名"或"该层组件"
- 答题：自下而上识别"采集→传输→处理"
    - 采集 = ==感知层==
    - 传输 = ==网络层==
    - 处理 = ==应用层==

### 3.4 真题与练习

#### 3.4.1 真题示例 1：MVx 模式选择（高频）

**题目**：某公司开发新一代电商前端，要求页面状态变化时 UI 自动同步刷新，开发团队熟悉 Vue.js。请说明应采用何种 UI 架构模式，并对比 MVC、MVP、MVVM 三种模式的核心差异。

**参考答题**：

> **理论**：层次式 UI 架构的三种主流模式为 MVC、MVP、MVVM，核心差异在 ==View 与 Model 的关联方式== 和 ==数据流向==。
>
> **分点对比**：
> （1）**MVC**（Model-View-Controller）：
>   - View → Controller → Model 单向数据流
>   - 适合 ==服务端 Web==（如 Spring MVC、Rails）
> （2）**MVP**（Model-View-Presenter）：
>   - Presenter 操控 View，View 完全被动
>   - 适合 ==Android 早期== 和富客户端，UI 测试性强
> （3）**MVVM**（Model-View-ViewModel）：
>   - ==双向数据绑定==：View 与 ViewModel 自动同步
>   - 适合 ==SPA、Vue / Angular / SwiftUI==
>
> **结论**：本场景"页面状态变化时 UI 自动刷新 + Vue.js"完全匹配 ==**MVVM 模式**== 的双向绑定特性，应采用 MVVM。

#### 3.4.2 真题示例 2：物联网 3 层填空（中频）

**题目**：某智慧城市物联网平台架构图（自下而上 3 层），请填空：
- (1)：____ — 采集环境数据，使用 RFID、温湿度传感器
- (2)：____ — 数据传输，使用 5G、NB-IoT、LoRa
- (3)：____ — 业务处理，提供智慧交通、智能照明等服务

**参考答题**：

> **理论**：物联网采用 ==3 层架构==：感知层、网络层、应用层（自下而上）。
>
> **分点填空**：
> （1）**感知层**：负责数据采集，对应 RFID、传感器
> （2）**网络层**：负责数据传输，对应 5G、NB-IoT、LoRa 等
> （3）**应用层**：负责业务处理，对应智慧交通、智能照明等服务
>
> **总结**：物联网架构按"采集 → 传输 → 处理"对应感知/网络/应用 3 层。

---

## 四、风格规范（与 E2-02 一致）

### 4.1 Block ID 规范

- `^4-layers` — 4 层架构职责
- `^strict-vs-loose` — 严格 vs 松散分层
- `^mvc-mvp-mvvm` — MVx 三者对比
- `^iot-3layers` — 物联网 3 层架构
- `^mvx-keywords` — MVx 关键字判定
- `^realexam-mvvm` — 真题 1 MVVM
- `^realexam-iot` — 真题 2 物联网填空

### 4.2 关联链接（沿用骨架）

- 返回案例分析索引：`[[00-案例分析解题框架]]`
- 返回主索引：`[[../MOC]]`
- 关联综合知识章节：[[../01-综合知识/08-系统架构设计]] / [[../01-综合知识/07-系统设计]] / [[../01-综合知识/03-计算机网络]]

---

## 五、预期产出

- **文件**：`02-案例分析/03-层次式架构设计.md`（骨架 111 行 → 细化 280-350 行）
- **考点覆盖**：
  - 对比表 ≥4 个：4 层架构、严格 vs 松散、MVx 三者、物联网 3 层、关键字判定
  - 真题示例 ≥2 个
  - Block ID 锚点 ≥7 个

---

## 六、实施流程

1. 重写 `02-案例分析/03-层次式架构设计.md`
2. 用户验证（Obsidian 渲染、内容核对）
3. 通过验证后，更新 [revision-task-tracker.md](../../audit/revision-task-tracker.md) 追加日志
4. 两次提交：
   - `docs(E2-03): 细化层次式架构设计专题`
   - `chore(E2-03): 更新任务日志，层次式架构设计细化完成`

---

## 七、风险与注意

- **MVC/MVP/MVVM 区分必须清晰**：View-Model 关系是核心区分点
- **物联网 3 层不要画错方向**：自下而上 = 感知 → 网络 → 应用
- **不重复物联网网络细节**：5G / LoRa / NB-IoT 详见 [[../01-综合知识/03-计算机网络]]
- **真题选取**：MVVM 选型 + 物联网填空都是历年高频题模板
