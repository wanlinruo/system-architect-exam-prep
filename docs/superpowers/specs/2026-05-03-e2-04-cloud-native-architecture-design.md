---
任务编号: E2-04
任务名称: 细化 04-云原生架构设计.md
对应专题: 云原生架构设计
重点级别: 高频
设计日期: 2026-05-03
状态: 待实施
---

# E2-04 云原生架构设计 细化设计文档

## 一、背景与目标

### 1.1 任务来源

依据 [E2 框架阶段计划](../plans/2026-04-30-e2-case-analysis-framework.md)，按方案 1 优先级（高频先行）对 `02-案例分析/04-云原生架构设计.md` 进行细化。

### 1.2 范围决策

经与用户确认，采用 **方案 A 标准细化**：沿用骨架 X.1-X.4 结构展开，含 1-2 道历年真题。

### 1.3 成功标准

- 覆盖云原生全部高频考点：CNCF 4 大特征、Docker、K8s、服务网格、Serverless、微服务设计原则、12-Factor
- 含 1-2 道真题（单体→微服务、服务网格 vs API Gateway）
- 与 E2-02/03 风格一致
- 行数约 320-400 行（骨架 129 → 细化版 320+）

---

## 二、文件组织

### 2.1 文件路径

- 路径：`02-案例分析/04-云原生架构设计.md`
- 操作：基于现有骨架 ==重写==（保留 frontmatter / 标题 / warning，扩充 mindmap 与 4.1-4.4）

### 2.2 完整结构

```
# 云原生架构设计
├── (frontmatter)
├── ## 知识全景         — Mermaid mindmap
├── ## 4.1 核心概念
│   ├── 4.1.1 CNCF 4 大特征
│   ├── 4.1.2 演进路径（单体 → SOA → 微服务 → 云原生）
│   └── 4.1.3 12-Factor 应用原则
├── ## 4.2 关键技术与架构
│   ├── 4.2.1 容器（Docker）
│   ├── 4.2.2 编排（Kubernetes）
│   ├── 4.2.3 服务网格（Istio）
│   ├── 4.2.4 Serverless / FaaS
│   └── 4.2.5 微服务设计原则
├── ## 4.3 典型考点与解题套路
│   ├── 4.3.1 单体 → 微服务 改造模板
│   ├── 4.3.2 关键字判定速查
│   └── 4.3.3 服务治理 vs Service Mesh 选择
├── ## 4.4 真题与练习
│   ├── 4.4.1 真题 1：单体改微服务
│   └── 4.4.2 真题 2：服务网格 vs API Gateway
└── ## 关联链接
```

---

## 三、各节内容设计

### 4.1 核心概念

#### 4.1.1 CNCF 4 大特征
- **容器化**（Containerization）— 应用打包到容器
- **微服务**（Microservices）— 单一职责、独立部署
- **DevOps**（开发运维一体化）— 文化与工具
- **持续交付**（Continuous Delivery / CI-CD）— 自动化流水线

#### 4.1.2 架构演进路径
单体 → SOA → 微服务 → 云原生（容器+编排+服务网格）

| 阶段 | 关键特征 |
|------|---------|
| 单体 | 一个应用一个进程一个数据库 |
| SOA | 服务化、基于 ESB |
| 微服务 | 细粒度、去中心化、独立数据库 |
| 云原生 | 容器化 + K8s 编排 + 服务网格 |

#### 4.1.3 12-Factor 原则（适合 SaaS 应用）

12 条原则（精简版）：
- 基准代码、依赖显式声明、配置与代码分离、后端服务作为附加资源、构建/发布/运行严格分离、进程无状态、端口绑定、并发通过水平扩展、快速启动优雅终止、开发与生产对等、日志即事件流、管理任务独立进程

**关键 callout**：
- `> [!important]` CNCF 4 大特征
- `> [!tip]` 12-Factor 简记口诀

### 4.2 关键技术与架构

#### 4.2.1 容器（Docker）

| 概念 | 说明 |
|------|------|
| **Image** | 只读模板（含应用 + 依赖 + 运行时） |
| **Container** | Image 的运行实例 |
| **Registry** | 镜像仓库（如 Docker Hub、Harbor） |
| **Dockerfile** | 镜像构建脚本 |

容器 vs 虚拟机：
- 容器：共享 OS 内核、轻量、秒级启动
- 虚拟机：独立 OS、隔离强、分钟级启动

#### 4.2.2 编排（Kubernetes / K8s）

| 控制平面组件 | 职责 |
|------|------|
| **API Server** | 唯一入口、所有操作通过 API |
| **etcd** | 分布式 KV 存储（集群状态） |
| **Scheduler** | 调度 Pod 到合适节点 |
| **Controller Manager** | 维持期望状态 |

| 工作负载概念 | 说明 |
|------|------|
| **Pod** | 最小调度单元（1+ 容器） |
| **Deployment** | 声明式管理 Pod 副本 |
| **Service** | 服务发现 + 负载均衡 |
| **Ingress** | 7 层路由（HTTP / HTTPS） |
| **ConfigMap / Secret** | 配置 / 敏感信息管理 |

#### 4.2.3 服务网格（Service Mesh / Istio）

核心思想：==控制平面与数据平面分离==

| 平面 | 组件（Istio） | 职责 |
|------|------|------|
| **数据平面** | Envoy 代理（Sidecar） | 拦截服务间所有流量、实施策略 |
| **控制平面** | Pilot / Citadel / Galley | 配置下发、证书签发、配置校验 |

**3 大能力**：流量管理 / 安全（mTLS）/ 可观测性

#### 4.2.4 Serverless / FaaS

- **FaaS**（Function as a Service）：函数即服务，按调用付费
- 特征：==事件驱动 / 自动扩缩容 / 无状态 / 无服务器管理==
- 典型：AWS Lambda、阿里云函数计算、Cloudflare Workers
- 适用：突发流量、异步任务、事件处理

#### 4.2.5 微服务设计原则

| 原则 | 说明 |
|------|------|
| **单一职责** | 每个微服务负责一个业务能力 |
| **自治性** | 独立部署、独立技术栈、独立扩展 |
| **数据独立** | 每个服务拥有自己的数据库 |
| **故障隔离** | 单个服务故障不影响整体 |
| **去中心化治理** | 不依赖单一总线（vs SOA 的 ESB） |
| **基础设施自动化** | CI/CD、容器化部署 |

**关键 callout**：
- `> [!important]` Docker / K8s / Istio 概念表
- `> [!important]` 微服务 6 大设计原则
- `> [!warning]` 容器 vs 虚拟机区分

### 4.3 典型考点与解题套路

#### 4.3.1 单体 → 微服务改造模板

> 第一段（理论）：微服务架构通过将单体系统按 ==业务能力== 拆分为细粒度独立服务，实现 ==独立部署、独立扩展、技术异构==。
>
> 第二段（分点）：
> - （1）拆分原则：按业务领域 DDD、按读写分离、按变化频率
> - （2）数据拆分：每个服务独立数据库，避免共享 DB
> - （3）服务治理：服务注册发现、负载均衡、熔断降级
> - （4）部署运维：容器化（Docker）+ 编排（K8s）+ CI/CD
>
> 第三段（结合场景）：本系统的痛点是 <场景>，改造后通过 <X 技术> 解决。

#### 4.3.2 关键字判定速查

| 题目关键字 | 推荐架构 |
|----------|---------|
| 快速迭代 / 独立部署 / 技术异构 | **微服务** |
| 突发流量 / 事件驱动 / 按调用付费 | **Serverless / FaaS** |
| 服务间通信治理 / mTLS / 可观测 | **服务网格 Istio** |
| 容器编排 / 自动扩缩容 / 滚动更新 | **K8s** |
| 应用打包 / 环境一致性 | **Docker** |

#### 4.3.3 服务治理 vs Service Mesh 选择

| 维度 | 传统服务治理（Spring Cloud） | Service Mesh（Istio） |
|------|---------------------------|----------------------|
| 治理位置 | ==SDK 嵌入业务代码== | ==Sidecar 透明拦截== |
| 语言耦合 | 强（绑定 Java/Spring） | 无（语言无关） |
| 升级成本 | 高（需改业务代码） | 低（只升级 Sidecar） |
| 学习曲线 | 中 | 高 |

### 4.4 真题与练习

#### 4.4.1 真题示例 1：单体 → 微服务改造（高频）

**题目**：某电商系统采用单体架构，随着业务扩张，出现以下问题：
- 一次发布需停机 1 小时，无法做到高频迭代
- 数据库压力大，单库 200GB
- 团队从 5 人扩张到 60 人，开发互相阻塞

请说明如何将系统改造为微服务架构，并指出改造的风险。

> [!example]+ 参考答题
>
> **理论**：微服务架构通过将单体系统按 ==业务能力==（如商品、订单、支付、库存）拆分为细粒度独立服务，实现独立部署、独立扩展。
>
> **改造方案分点**：
> （1）**服务拆分**：
>   - 按业务领域（DDD）拆分：商品 / 订单 / 支付 / 库存 / 用户 / 营销
>   - 优先拆变化频率高、瓶颈服务（如订单）
> （2）**数据拆分**：
>   - 每个服务独立数据库（避免共享）
>   - 用 ==分布式事务== 或 ==Saga== 模式处理跨服务一致性
> （3）**服务治理**：
>   - **服务注册发现**：Nacos / Eureka
>   - **负载均衡**：Ribbon / Service Mesh
>   - **熔断降级**：Hystrix / Sentinel
>   - **API 网关**：Spring Cloud Gateway / Kong
> （4）**部署运维**：
>   - **容器化**：Docker 镜像统一打包
>   - **编排**：K8s 管理 Pod、自动扩缩容
>   - **CI/CD**：GitLab CI / Jenkins，独立流水线
>
> **风险**：
> - 分布式事务复杂度上升
> - 服务间网络通信增多，==延迟敏感==
> - 监控运维成本提升（需要 ==链路追踪== 如 SkyWalking、Jaeger）
>
> **结论**：通过按业务能力拆分 + 容器化 + 服务治理，解决单体的高频迭代与扩展瓶颈。

#### 4.4.2 真题示例 2：服务网格 vs API Gateway（中频）

**题目**：在微服务架构中，团队对"服务治理用 Spring Cloud SDK 还是 Service Mesh"存在分歧。请对比两种方案，并给出选型建议。

> [!example]+ 参考答题
>
> **理论**：服务治理是微服务架构的核心问题。主流方案有两种：基于 SDK（如 Spring Cloud）和基于 Service Mesh（如 Istio）。
>
> **分点对比**：
> （1）**SDK 方案（Spring Cloud）**：
>   - 治理逻辑 ==嵌入业务代码==（依赖 SDK）
>   - 优点：成熟度高、性能好（无 Sidecar 转发）
>   - 缺点：==与编程语言强绑定==（Java/Spring 生态）；升级需改业务代码
> （2）**Service Mesh 方案（Istio）**：
>   - 治理逻辑 ==下沉到 Sidecar==（Envoy 代理）
>   - 优点：==语言无关==、业务无侵入、统一治理面
>   - 缺点：学习曲线高、Sidecar 增加 ==网络延迟==
> （3）**API Gateway 不同**：
>   - API Gateway 是 ==南北流量==（外部 → 内部）的入口
>   - Service Mesh 是 ==东西流量==（服务 → 服务）的治理
>   - 两者 ==互补==，不互斥
>
> **结论**：
> - 如果团队主要用 Java/Spring 且追求成熟稳定 → **SDK 方案**
> - 如果有 ==多语言异构==（Java + Go + Python） → **Service Mesh**
> - 大多数生产实践：**API Gateway 处理南北流量 + Service Mesh / SDK 处理东西流量**

---

## 四、风格规范（与 E2-02/03 一致）

### 4.1 Block ID 规范

- `^cncf-4features` — CNCF 4 大特征
- `^evolution-path` — 架构演进路径
- `^docker-concepts` — Docker 概念表
- `^k8s-components` — K8s 控制平面 + 工作负载
- `^istio-planes` — Istio 数据/控制平面
- `^microservice-principles` — 微服务 6 原则
- `^cloud-native-keywords` — 关键字判定
- `^sdk-vs-mesh` — SDK vs Service Mesh
- `^realexam-monolith-to-ms` — 真题 1
- `^realexam-mesh-vs-gateway` — 真题 2

### 4.2 关联链接（沿用骨架）

- 返回案例分析索引：`[[00-案例分析解题框架]]`
- 返回主索引：`[[../MOC]]`
- 关联综合知识章节：[[../01-综合知识/08-系统架构设计]] / [[../01-综合知识/03-计算机网络]] / [[../01-综合知识/16-其他知识]]
- 关联案例分析：[[05-面向服务架构设计]]（SOA 与微服务对比）

---

## 五、预期产出

- **文件**：`02-案例分析/04-云原生架构设计.md`（骨架 129 行 → 细化 320-400 行）
- **考点覆盖**：
  - 对比表 ≥6 个：CNCF 4 特征、演进路径、Docker、K8s、Istio、微服务原则、关键字判定、SDK vs Mesh
  - 真题示例 ≥2 个
  - Block ID 锚点 ≥10 个

---

## 六、实施流程

1. 重写 `02-案例分析/04-云原生架构设计.md`
2. 用户验证（Obsidian 渲染、内容核对）
3. 通过验证后，更新 [revision-task-tracker.md](../../audit/revision-task-tracker.md) 追加日志
4. 两次提交：
   - `docs(E2-04): 细化云原生架构设计专题`
   - `chore(E2-04): 更新任务日志，云原生架构设计细化完成`

---

## 七、风险与注意

- **避免技术细节过深**：考试不考 K8s YAML 写法、Istio CRD 配置，只考概念识别
- **服务网格 vs API Gateway** 是高频易混点，重点强化
- **微服务 6 原则**作为答题骨架可复用到其他题目
- **不重复 SOA**：SOA 详见 [[05-面向服务架构设计]]，本章侧重微服务对比
